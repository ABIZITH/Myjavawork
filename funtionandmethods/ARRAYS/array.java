package ARRAYS;

public class array {            // array is a collection of datatype  
                               // all the datatype in a array should be same 
    public static void main(String[] args) {
        // syntax  
        //datatype[] variable name = datatype[size]
        
        int[] roll = new int[5];      // roll is defined in the stack ,roll object is created in the memory(heap memory)
        
        
    } 
}
